"""Dashboard summary aggregation.

TODO(phase5): build_summary(db, user_id) -> dict for SummaryOut, in as few
queries as possible: service counts by status, active releases, open incidents,
average recent latency, rollback-ready release count.
"""
