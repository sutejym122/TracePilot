"""Release + checklist schemas.

TODO(phase3):
  ReleaseCreate, ReleaseUpdate, ReleaseOut,
  ReleaseDetailOut(ReleaseOut + checklist + incidents[]),
  ChecklistUpdate (the five booleans, optional),
  ChecklistOut (five booleans + computed score + is_ready)
"""
